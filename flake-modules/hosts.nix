# Public-scope host declarations.
#
# The public flake ships no sample host. Downstream flakes declare their own
# hosts with real identity through `easy-hosts.hosts`.
_: {
  easy-hosts.hosts = { };
}
